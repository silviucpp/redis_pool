{application, redis_pool, [
    {description, "erlang pool for redis based on eredis and erlpool"},
    {vsn, "1.0"},
    {registered, []},
    {applications, [kernel, stdlib, eredis, erlpool]},
    {mod, {redis_pool_app, []}},
    {env, []}
]}.